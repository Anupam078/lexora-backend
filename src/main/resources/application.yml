spring:
  application:
    name: lexora-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/lexora_db
    username: postgres
    password: Aojha@321
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080